[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "env_explorer"
version = "0.1.0"
description = "Env Explorer - Codebase for 'Inducing Pareto-Optimal Environment Exploration for LLM Agents via Reasoning'"
requires-python = ">=3.8"